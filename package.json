{
  "name": "carto-residence",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web",
    "build": "npm run build --workspace apps/web",
    "test:scoring": "python3 -m unittest discover -s tests",
    "validate:catalog": "python3 scripts/validate_catalog.py data/criteria/prototype_catalog.json"
  }
}
