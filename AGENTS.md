# AGENTS.md

## Cursor Cloud specific instructions

This repository (`cartov2`) is currently empty — it contains only a `README.md`. There is no application code, build system, package manager, or test framework configured yet.

- **No dependencies** to install.
- **No services** to start.
- **No lint, test, or build commands** available.

Once application code is added, update this section with service startup instructions, testing caveats, and any non-obvious development environment notes.
